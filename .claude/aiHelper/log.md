# Agent Log — Greek Word Games Platform

> Newest-first. Full detail for the two most recent sessions only; everything older is a one-line row in the table.
> **Rule (2026-07-18):** keep this file under **120 lines** — the end-of-session Dream (soul.md) demotes older entries and era-groups the oldest rows. Git history keeps the full text.

---

## Session 146 — 2026-08-11: the Platform makes a noise — TICKET-04 built

Five TDD slices, and the design ADR 0021 wrote held up unchanged: `selectSoundCue` is three lines,
the reducer was never touched, and `lastSubmission` was already the event source. New files are
`config/sound.ts` (Cue registry + fixed per-Cue volumes + the provenance block `TICKET-05` fills),
pure `leksokipos/lib/soundCue.ts`, `useSoundEnabled` (a `useTheme` clone on key `sound-preference`,
the third standalone localStorage carve-out) and `useSoundCue` (one lazy `Audio` per Cue, restart
not stack). Toggle 🔊/🔇 inline in `Shell.tsx` beside ☀️/🌙 — the header is four buttons wide on
mobile now, and **nothing guards that**: ADR 0021 named `mobileLayout.test.tsx`, which renders
`HowToPlayModal` and has never touched the Shell, and jsdom has no layout engine so no test here
could. Moved to `TICKET-05`'s done-when, which already puts the operator on a phone.

**The ticket's own jsdom instruction was wrong, and it was the s132 trap wearing a new hat.** It
said to stub `HTMLMediaElement.play` "guarded the same way the `scrollIntoView` stub is" — but
`if (!Element.prototype.scrollIntoView)` works only because jsdom genuinely lacks that method.
jsdom **does define `play()`**: it logs "Not implemented", returns **`undefined`**, and the guard
therefore never fires. Probed rather than assumed, which mattered twice — the stub had to become
unconditional, and the return value being `undefined` rather than a Promise is exactly what killed
Offline Mode in s132 (`router.prefetch` returns void). The hook ships `audio.play()?.catch(() => {})`
and a test pins each half separately, because older Safari does the same thing a real browser never
documents. For the same reason `useSoundCue.test.ts` subclasses the real `Audio` instead of
substituting a fake one: a hand-rolled Audio mock would be a claim about the contract, and the
claim is what has been wrong every previous time.

`selectSoundCue` extended `gameLogic.test.ts` rather than opening a file, per the ticket's own
grep-the-coverage-map instruction, and every case is driven through the **real** `validateWord` —
a hand-built `ValidationResult` could drift from the validator in silence. Gates: **192 files /
2450 tests**, eslint 0, build 0, e2e **7 passed / 2 skipped** after clearing `.next`. The five
`rlsInvariantsLiveDb` failures stayed gone. `TICKET-04` deleted; `TICKET-05`'s deploy gate rewritten
to say what is now true — **the 🔊 button renders on every page and plays silence**, so `dev` is
merged-and-undeployed until the three MP3s land.

## Session 145 — 2026-08-10: Sound Cues designed — ADR 0021 + two tickets, no code

The Platform has shipped eleven Games and has never made a sound: zero audio in `src/`, no audio dependency, `public/` holds only SVGs. An operator request for three Leksokipos sounds (rooster on Pangram, click on Word found, sarcastic clap on `missing_center`) was grilled to nineteen closed decisions and landed as **ADR 0021 + `TICKET-04` (the primitive) + `TICKET-05` (the three MP3s)**. No implementation.

**The design needed almost no new machinery, and that was the finding.** `ValidationStatus` already names all six submission outcomes, and `lastSubmission` is a fresh object per submit that is deliberately not persisted — so the event source already existed. The reducer stays untouched: a pure `selectSoundCue(result)` in `games/leksokipos/lib/` plus one `useEffect` in `GameBoard` is the entire seam. Cues are named for **the moment, not the noise** (`pangram`/`wordFound`/`missingCenter`), so swapping the rooster later is a file replace, not a rename.

**Two claims would have entered the repo as facts if unchecked.** First, **Pixabay is not CC0** — it is the Pixabay Content License (no attribution, commercial fine, but no redistribution "on a standalone basis"), while every casual reference calls it CC0. It still clears the bar, since a bundled MP3 is not standalone distribution, but it must be *recorded* as Pixabay. CC-BY is refused outright: it obliges a permanent credit line in the How-to-Play modal, which is exactly why `topothesies/attribution.ts` exists, and three tiny sounds do not justify one. Second, **ADR 0020 landed two days earlier and appeared to contradict the design** — "behaviour enrols" would make sound a `GameCapability`. Its own stated criterion settled it: behaviour is *what a Game does to the shared database*, and a Cue writes nothing, so it sits with Offline Mode and drawer section, which that ADR already names as presentation.

**The artifact asked for was a handoff; the correct one was an ADR.** This repo's rule is that handoffs hold *open questions*, and none survived the grill. Sound Cues is genuinely architecture: a Platform primitive, a permanent third `localStorage` key outside the envelope, a licensing bar, and a frozen cue vocabulary. Also settled: off by default (mobile blocks the first play anyway, so on-by-default is both unreliable and rude), the toggle inline beside ☀️/🌙 with the icon as its own discoverability, iOS's silent switch accepted with no workaround, and **no test may assert audibility** — nothing in this stack can. Pre-launch but explicitly **non-blocking**, since the launch checklist is itself still an open question.

Docs only, three files, no gates owed. Two corrections caught during the Dream, both from the same cause — two sessions had logged between this one's context read and its writes: the entry is **145, not 143**, and the tickets are **04/05, not 01/02**, because 01–03 are spent and numbers are never reused. Announced, not fixed: **`ISSUE-02` is cited by `memory.md` and `goals.md` but no file exists in `.claude/tracker/issues/`.**


## Older Sessions

| Session | Date | Summary |
|---------|------|---------|
| 144 | 2026-08-10 | **Every emoji badge becomes a drawn mark in a tier frame** (TICKET-03, test-first in five slices). `glyph`, `TIER_MEDALS` (🥉🥈🥇💠), the 🔒 fallback, the `grayscale` class and the toast's fixed 🏆 all deleted for one `BadgeMark` + **five drawings**: a ring in the tier's strong colour, a soft disc, a mark always `currentColor` — so a tier is a *colour*, never a different picture, which is why five drawings cover four tiers across three surfaces (chip 14px / tile 32px / toast 34px). Eight `--tier-*` tokens **verified compiled out of the production CSS bundle** rather than assumed (a missing token renders no ring and every test still passes). **The ticket's flagged check paid out:** `EarnedToast` carries `id`, but TICKET-02 made every badge tiered so that id is always a *tier* id → `achievementById` returns undefined on every unlock; and `tierLabel` is Greek display copy that cannot map back to a colour, so without a new `tier: TierName` field the toast draws the **neutral** frame — an unlock rendering as *locked*, the kind of bug every gate passes. Two decisions moved from prose into tests: *changes only the frame between tiers* (same path `d` across two tiers) and *keeps the mark visible when locked*. `achievementToast.test.tsx` rewritten to build every fixture through `describeAchievement` — it had been carrying the retired `leksokipos-first-daily` id, the s140 pattern exactly. 13 files; 191/2430 green; **the 5 `rlsInvariantsLiveDb` failures are gone** (migration pushed between s142 and now, suite 30/30). Still owed: an operator eye-check of the Trophy Case in both themes. |
| 143 | 2026-08-10 | **Badge visual system designed end-to-end; both badge handoffs retired** (docs only, zero source files). `badgeIdeas.md` was **discharged and should already have been deleted** at s140's Dream; `badgeVisualSystem.md` was live but **stale on three facts** (Τζιμάνι 80%, pangram tiers 10/20/50, citing the superseded 08-06 amendment). **The durable lesson is the operator's method:** two batches had been answered in prose when the instruction came — *no visual decision gets made in text, put every option in HTML and let me look*. Every agreed visual answer was reopened as a rendered comparison, and **one answer changed on sight** (locked: greyscale filter → neutral frame). **Prose grilling is the wrong instrument for anything whose failure mode is "looks wrong at 12px"**; `.claude/aiHelper/html/` is now the standing home for look-at-it artifacts. Reading the code settled two things the grill could not: `FlowerGrid` is six petals + a centre disc, so a *complete* flower literally is the seven-letter set = a pangram (a real fork, not a preference; honeycomb retired outright), and `TierChips` are text-only, so **four of nine catalogue emoji had never rendered anywhere**. One storage-shape catch before the ticket: `mark: {path, viewBox}` (topothesies precedent) cannot express six rotated ellipses → every mark **flattened to one path**, and the spec page renders the flattened version, so what was reviewed is what ships. Settled: circle frame, ring = size/10, mark 66% and always `currentColor` (**five drawings, not nineteen**), Palette A / eight tokens, Διαμάντι as hue only, `TIER_MEDALS` + every `glyph` deleted. Landed as `TICKET-03` + an ADR 0013 §7 amendment; both handoffs deleted, five references repointed. |
| 142 | 2026-08-08 | **`isISODate` graduates to `src/lib/puzzleDate.ts`** — an operator proposal whose move was right and whose stated reasons mostly were not. Both headline wins (edge bundles, cold-parse CPU) are worth ~zero: every symbol the leksokipos barrel re-exports is a small pure function, so tree-shaking already dropped them. **What actually justified it went unmentioned: `ISO_DATE_RE` was duplicated verbatim** in `puzzleDate.ts` and `leksokipos/lib/puzzle.ts` — one rule, two copies, in two modules a single route imported two lines apart. **No re-export shim**, deliberately unlike the `normalizeLetters` precedent the proposal cited: nothing inside the game used it, so a shim would be dead on arrival. `isDailyPuzzle` stays (it takes a puzzle and keys off an unanchored prefix rule). **The expensive lesson was in the gates:** e2e failed on `/` with `Unexpected end of JSON input`, s140 had logged that flake as "clean on re-run", so re-run stability was read as proof it was real → a stash-and-bisect for nothing. **The stale Turbopack chunk survives re-runs**; `Remove-Item .next` is the distinguishing test. Also recorded: the 5 `rlsInvariantsLiveDb` failures and the e2e suite validate the migration only, never the deploy — they go green whether or not Vercel shipped. |
| 141 | 2026-08-07 | **Six game pages collapse onto one `GamePageChrome`** — an operator proposal evaluated first, then rescoped. **Four of its claims failed measurement:** it counted 5 duplicate page clients (there are **6**, Vres Tin Frasi was missed); it scoped itself to the slot-fill family to avoid ADR 0019 while **listing Λεξοδρομία, the game that ADR names as a permanent non-member** — page chrome is simply not the round-spine axis; "five files become one" is impossible because Topothesies declares five board props and Πόσο κάνει; one, so a config object needs `any`; and "the adapters differ only in type names" holds for two of three, so **the adapters were left alone**. Shipped: `GamePageChrome` owning the title row, both header triggers, and the **Session-key remount** (a five-line comment pasted above each board became one `<Fragment key={sessionKey}>`); board and rules modal arrive as render props. `GameHelpButton` single-sources the `?` trigger and unified a stray English `aria-label` onto the Greek six pages used. **Leksiarxeio deliberately excluded** — two *server* components render its rules modal, so joining would buy 20 lines and cost a dual-mode component; the honest boundary is smaller than the tidy one. −255/+383 over 13 files; page clients ~58→~30 lines. Gates 2386/2391 (the 5 documented `rlsInvariantsLiveDb` failures await the un-pushed migration), e2e 7 passed / 2 skipped. |
| 140 | 2026-08-07 | **Badge catalog rebuilt to five tiered badges + the launch reset** (TICKET-02, `/tdd`). Στην Κορυφή (1/10/25 top-rank days), Μακρυλέξης (unchanged), Τζιμάνι (1/5/10 days at 70%), Κυνηγός Πανγκράμ (**25/60/150**, raised), Συλλέκτης Πόντων (unchanged); Πρώτα Βήματα deleted, **Θεριστής retired and `leksokipos-tzimani` revived** — an id and a name that disagree would disagree forever. Three frozen-id exceptions, licensed by the pre-launch wipe and nothing else. **Structural change: `detectEarnedAchievements` returns word-length ids and nothing else** — every other badge awards a *number of days*, a lifetime count only the server holds, so they moved onto the pangram lane's shape (round records the day, POST returns the count, crossing decided off that). Four badges derive rungs from one `metalTiers(baseId, thresholds)` helper so a tier id can't be mistyped into a permanent junk row. Six TDD slices; **blast radius of 8 test files** where retired ids had been arbitrary valid strings — two swaps were substantive (no one-shot entries survive, so those cases became two-tiered-badge cases), and a blanket `sed` wrongly rewrote the *selected* id, which always stores the BASE id. Best new test came from the ticket's own safety claim: a retired selected id must resolve to null (34 devices had Πρώτα Βήματα selected). Lesson: **loose API stubs crash the page rather than failing the assertion** — an unguarded `.map`/`toLocaleString` threw mid-render and the symptom was an empty body, reading exactly like "the section was never added". Also shipped `launch-reset.sql` (committed, never auto-applied), ADR 0012's append-forever exception, ADR 0013's 2026-08-07 amendment, CONTEXT's Τζιμάνι entry. Filed `ISSUE-02` for a sixth intermittent `game_state` DELETE failure under full-suite load. |
| 139 | 2026-08-07 | **`player_milestones` — one table, one route, one merge** (TICKET-01, `/grill-with-docs`→`/tdd`). `player_pangrams`+`player_words` dropped into `player_milestones(device_uuid, puzzle_date, kind, detail, value)` with the two day counters nothing previously captured; `POST /api/milestones` replaces two routes; `planMilestoneMerge` replaces two planners; fifth sync lane. Migration `20260807120000` **committed, NOT pushed**. **The grill found two spec errors.** The cold drop was justified by "the word capture is dark behind `FEATURE_FLAGS.achievements`" — that flag has been `true` since s112, so the capture was live; the drop was still right (no earned row depends on those rows) but the operator decided it knowingly. The durable one: **`theristisFoundRatio` had to move 0.8→0.7 in THIS ticket, not TICKET-02** — milestone rows are written only as days are played, so every qualifying day in the gap would be permanently unrecorded; the immutability rule ("lower is safe") is about *earned* rows and says nothing about *unwritten* ones. Same reasoning put the achieved percentage in `value` — free on a row already being written. Cost fix: counts returned only for kinds that actually inserted, plus **client-side ≥10 word filter** — ~30 requests a game → under 4 a day, both riding `.select("kind")` after the upsert. Nine TDD slices; new pure seams `planMilestoneMerge`/`sanitizeMilestones`/`detectDayMilestones`. |
| 138 | 2026-08-06 | **Documentation service — every doc cross-checked against the code.** Worst find was not a doc bug: **`leksindeseis` has carried `wip: true` since the registry was written and was never flipped**, so a finished game renders under «Υπό κατασκευή» while four docs called it Live — operator ruled the code right, the docs wrong. Second: the 2026-07-31 wayfinder migration deleted issues 12–15 and nothing repointed the docs, so «Πόσο κάνει;» content sourcing was genuinely untracked; pointers now name the surviving handoff. Third: `.claude/handoffs/` was invented after several docs were written, so five files pointed at a path that never existed. Also fixed README's game table/architecture tree/slash-command table, the 85%→`SCORE_SCALE` 0.75 drift in README+CONTEXT, CONTEXT's table count and Phrase bound, ADR 0018's CC-BY title after the ODbL swap, `soul.md`'s wrong test path, `skillsNotes.md`'s skill count. **s137 had no log entry** (that session skipped its Dream) — reconstructed from the commit and flagged second-hand. Link-checker: 0 broken relative links. |
| 137 | 2026-08-06 | **Podium badges rejected; the catalog rebuilt as a spec, nothing built** (`/grilling`; entry reconstructed in s138 — the session skipped its Dream). Ticket 08 asked which podium tiers to build; the answer was **none**, and the whole lane went decided-for-deletion including the cross-device `/api/profile/stats` query, retiring a scaling risk with the feature. The reason is structural: **podium slots are fixed at three while the audience grows**, so any top-N badge gets strictly harder over time and no threshold fixes it; percentile is audience-proof but at 8 players/day the top 10% is one player — harsher than first place. Measured first: 44 days, 365 scores, 8.3 players/day. The follow-on review settled far more: `player_milestones` absorbs the two fact tables and carries two new counters; Στην Κορυφή and Θεριστής→Τζιμάνι become tiered; Πρώτα Βήματα removed, `leksokipos-tzimani` revived (frozen-id exceptions 2 and 3, licensed only by the pre-launch wipe); one displayed badge permanently; emoji glyphs retired for drawn SVG. Governing principle: earned rows are immutable, so a threshold can be **lowered** (grants retroactively) but never effectively **raised** — **err high**. Landed as ADR 0013's 2026-08-06 amendment + `badgeIdeas.md` (supersedes it on four points) + `badgeVisualSystem.md`. |
| 136 | 2026-08-06 | **Topothesies reaches 109 answers; ticket 05 and its handoff retired** (`/grill-with-docs`). The ticket's premise had been wrong for weeks: neither a `place=island` override nor connected-component splitting was needed, because **a δήμος spanning several islands already arrives from OSM as one polygon per island** — the islands only had to be *selected*. Outlines were **measured before deciding** (106–243 points, inside the band of shapes already shipping), not assumed. **The build then broke twice on the same seam and only measurement caught either:** "capital's polygon then the nearest" gave Κουφονήσια a 0.15 km² rock; "capital first then descending area" gave it **Νάξος**, silently stripping the parent. Both passed the emitted-data gate clean — what exposed them was a nonsensical max-path-length line and a simplify bucket count off by one. The rule that works: **capital's polygon, then the largest polygons *smaller* than it**, extracted to pure `project.selectPeelPolygons`, which returns null so the generator throws rather than guesses. Δήλος is a permanent drop (no capital); Καστός a deliberate omission. Zero existing shapes or centroids moved (all 104 diffed against HEAD). ADR 0018 amended with everything durable the handoff held. |
| 134 | 2026-08-05 | **Community Puzzles get a scheduled release; serving stops destroying rows** (`/tdd`). Two defects in `consumeApprovedPuzzle`, both latent only because all three queues hold **zero** approved rows: the date was ignored (any date served the head of the queue), and consume **DELETEd** on every call while the loaders run per page load — so a refresh destroyed a puzzle and served the next. Built test-first in four slices around pure `nextFreeScheduledDate(taken, today)` (earliest free date **strictly after today**); consume became a non-destructive read keyed on `(status, scheduled_date)`; approve assigns the date (auto or validated admin override, **400 before any DB work**). Migration `20260805120000` = nullable column on the three consuming tables + **a partial unique index each**. **The type system found the design boundary:** `.eq("scheduled_date")` refused to compile because `CommunityPuzzleTable` includes Stavrolekso, which never consumes → new `ScheduledPuzzleTable = Exclude<…>`, so the exclusion is structural not conventional (ADR 0017 earning its keep). Verified against the live DB with a 2099 sentinel rather than trusting mocks; one stale mock surfaced and was the thing that was wrong. Operator settled the three design questions up front, so no grill was needed. |
| 133 | 2026-08-04 | **Vres Tin Frasi refused its own solution** (`/diagnosing-bugs`): confirmed at the source — the real pool + real reducer rejected the correct answer. The guess pool starts at `words-2.json`, so the single-letter «η» was in no pool at all; measuring scope found the other end too (pool stopped at 8, phrases ran to 15) → **142/498 phrases unsolvable**. Operator kept 8 as a hard ceiling and pruned 498→**411**. Key decision: `words-1.json` = `["η","ο"]` is AUTHORED and lives in `src/data/vrestifrasi/`, NOT beside the derived lists — the re-sync adapter rebuilds those from `words-el.json`, which has zero single-letter entries, so a derived file is rewritten empty and silently re-breaks every article-opening phrase. `phraseCorpusPlayable.test.ts` (drives the real reducer over the whole corpus) then caught 4 more no length filter could: a proper noun, an archaic form, a genitive, and **«Ό,τι» whose comma survives `normalizeLetters`**. Submit-modal copy now derives from gameRules — it had advertised a band 64 shipped phrases fell outside. Two process notes: a red suite on a clean tree was machine-load flake, not my change; and stashing revealed uncommitted Offline Mode removal work already in the tree, left untouched. |
| 132 | 2026-08-03 | **Offline Mode built (`/tdd`), then its core premise disproved (`/diagnosing-bugs`)** — scope cut to single-page protection. New `offlineOutbox.ts` (keyed `(gameId,puzzleDate)`, overwriting, keep-on-failure) + `postScoreAwaitable` + `useOfflineMode` context; drawer toggle, refresh guard, nav guard, day-change suppression. **`router.prefetch` returns `void`** — the awaited-`undefined` bug shipped because **my unit test mocked prefetch as promise-returning: I tested my assumption, not the API**. Worse: **`force-dynamic` payloads are never cache-served**, so cross-game offline is impossible by prefetch at all (proven dev + prod) → ADR 0010's reasoning invalidated, `e2e/offlineMode.spec.ts` kept `describe.skip` **failing on purpose** as the acceptance test. Also: coverageMap wasn't missing, it had MOVED — `git log` on a missing path reports the *directory*, so search by name before declaring an artifact gone. |
| 131 | 2026-07-30 | **Leksokipos difficulty rebalance — the plan's diagnosis was inverted.** Its tables all reproduced; the interpretation didn't: without `softCap` every puzzle demands exactly `scale × top%` of its own garden (spread 0.009), so the cap is not *a* contributor to small-vs-big unfairness but the **entire** source — big puzzles were the *fair* ones being made cheap. Cap stays anyway (removing it: words p90 68→214, max 728) — it trades fairness for a tedium ceiling. **`SCORE_SCALE` is the only knob reaching sub-knee puzzles** and the plan never searched it. Shipped scale 0.75 / k 400 / knee+80% threshold unchanged (moving the rank threshold would silently redefine the frozen `stin-korifi` badge). Corpus 1008→**799** via `puzzleQuality.ts` (imports the real scorers — never reimplements), dates reflowed to 2028-05-31; generators taught the gates or the next batch reintroduces what was deleted. Date-integrity test mandatory: `getPuzzleForDate` falls back to the LAST puzzle on a miss, so a gap serves 2028 silently. |
| 130 | 2026-07-27 | **Λογοπαίγνιο content sourcing pipeline** (ticket 03, scripts only, no app code): 3 npm commands (`logopaignio:fetch` Commons / `logopaignio:official` Plan B sites + Plan C favicons / `logopaignio:preview` generated eye-check page) merging one gitignored `_raw/manifest.json`; **106→159 candidates, 144 assets, 0 approved** — bottleneck moved from fetching to curation. Hard lesson: **Commons search returns confident nonsense** (ΔΕΗ→Namibia Power, ΣΤΑΣΥ→a Lithuanian choir) → require a distinctive accept-list token in the title else SUSPECT. Two of my own bugs caught by verification not tests (dup detector grouped 43 Plan B/C rows under `undefined`; an HTML error page saved as `logo.svg`). Never trust declared image sizes — measure bytes. Operator: Greek-origin rule DROPPED, sectors merged, **mark canvas 512×512** (a difficulty invariant — fixed-px blur erases a strip but hazes a square). |
| 128 | 2026-07-27 | **`useSlotFillRound` spine + ADR 0019** (`/tdd`): 3 cloned round hooks (posokanei/logopaignio byte-identical, topothesies same 6 rows) → one shared spine owning `useReducer`+`hasLiveActed` ref+dispatch wrapper+snapshot memo+`useRoundPersistence`; games pass only `{reducer,makeInitialState,toSnapshot,makeRestoreAction,hasProgress}`. Deliberate **sibling** of `useGuessRound`, not a merge (slot-fill posts *during* the round → needs `hasLiveActed`; guess family scores once on end). 11 tests on a synthetic reducer. Caught a self-inflicted regression: memoizing the snapshot on the whole `state` object writes to localStorage per keystroke → `useMemo` keyed on `Object.values(projected)`; the ref alternative is rejected by `react-hooks/refs`. CONTEXT.md +4 glossary terms. |
| 127 | 2026-07-26 | **Λογοπαίγνιο playable UI** (ticket 02, `/tdd`): real board replaces placeholder page, fully playable on the placeholder, still `wip:true`. New pure `blurRadiusForReveal` (blur px from `BLUR_STEP_RADII_PX`, clamps, 0 on reveal); `logopaignio/` components (`LogoReveal` framed+`filter:blur()`, `GuessTextInput` free-typed no-autocomplete, `Board`/`Result`/`GiveUpModal`/`HowToPlay` w/ trademark line+`credit`/`PageClient`); `useLogopaignioRound` = usePosokaneiRound clone; wired into scoreSubmission+leaderboard+`Shell GAME_IDS` (right «Υπό κατασκευή» drawer)+HomeTrophy. Remaining: ticket 03 real brands, 04 legal note+flip. |
| 126 | 2026-07-26 | **Λογοπαίγνιο foundation + pure logic** (ticket 01, `/tdd`, `wip:true` on a fake-brand placeholder — no real trademark): 5 TDD'd pure seams — `evaluateGuess` (`normalizeAnswer` = `normalizeLetters` + strip ALL whitespace, matched against a per-brand `accept[]` bridging the **Greek⇄Latin fork**), `selectDailyPuzzle` (date OPTIONAL → rotation keys on **id**, unlike posokanei), `computeScore` decay clone, reducer (flags DERIVED → RESTORE replays; `GIVE_UP`), `buildShareText` (fixed `MAX_GUESSES`-wide 🟦/🟩/⬜ grid). Config: registry row, `LOGOPAIGNIO` gameRules (`BLUR_STEP_RADII_PX` = ticket-02 difficulty knob), `SliceId`, rose accent. Minimal force-dynamic route. |
| 125 | 2026-07-26 | Profile toggle-back + word-length ladder + ≥10 `player_words` floor (`/grill-with-docs`→`/tdd`): header 👤 → `ProfileToggleButton` (opens /profile anywhere; on /profile `router.back()`, pure `resolveProfileNav`; `idx===0` deep-load → `/`); Σιδηρόδρομος → EXACT-length ladder (10/11/12/13 one-shots, `===` not `>=`, `achievementTuning.wordLengthBadges`, catalog generated); `POST /api/words` drops `<10` after sanitize; migration `20260726120000` DELETE `<10` (committed, NOT pushed); dissolves issue 14. |
| 124 | 2026-07-24 | **Πόσο κάνει; built** (`posokanei` 🛒, guess-the-price, `/tdd`, whole slice, `wip:true` on Αγγούρι placeholder): price-proximity logic (`evaluatePriceGuess` band+direction+proximity%), exact-date-else-rotation select, 100×guesses-left scoring, spoiler-free 🟩/🟨/⬛+arrows share, single-stage reducer, `formatEuro`; local `PriceInput` primitive; **`FramedMedia` graduated to shared/** (topothesies refactored onto it, 2 consumers); emerald accent; issue 13 tracks real content. |
| 123 | 2026-07-23 | Topothesies UX round 3 (operator review, LIVE): framed silhouette (`border-4 border-border rounded-card`); s121 on-map `StageReveal` → below-map `NameReveal` (green+🎉 correct, neutral-never-red wrong, always-green `btnApprove` next); capital round clears shape history (reminder kept, no red); autocomplete rows **select-only** + explicit green «Μάντεψε» commit (`resolveCurrent`) so a mis-tap can't burn a guess; `SHAPE_GUESSES` 4→5; global `scrollIntoView` stub in `setup.ts`. |
| 122 | 2026-07-23 | Λεξοδρομία decay bar replaces the ⏱️ points number (`/diagnosing-bugs`): reported "timer 2× faster on hard levels" was **not a clock bug** — the figure showed *points*, whose decay slope scales with `BASE_POINTS` (ascending 4→8-letter words → ~1.0→2.33 pt/s). Fix = redesign: pure base-independent `computeDecayFraction(elapsedMs)` (1→0 over 45 s) → reverse loading bar (`bg-game-accent`, empties fully) + `ελάχ. {floor}` label; scoring math untouched. Regression test locks bar base-independence. |
| 121 | 2026-07-22 | Topothesies UX round 2 + **PUBLISHED (`wip:false`, LIVE)**: autocomplete opens full list on focus; accent lime→ultramarine `blue-700`; per-stage on-map `StageReveal` gate (superseded by s123's below-map `NameReveal`); end screen stripped of on-screen emoji grid; dropdown alphabetical; `Modal` ✕→`btnHeaderIcon`; added `topothesies` to `Shell.tsx GAME_IDS` + `HomeTrophyButton`. |
| 120 | 2026-07-22 | Topothesies «Νομοί και Νησιά» reconciliation + deferred backlog → fidelity work-surface: **Θεσσαλονίκη→Χαλκιδική** merge (76→75 live), 7 backlog islands promoted → **29 deferred** (+6 `CANT_PEEL_PLACEHOLDERS`), `TOPO_PREVIEW=1` build → `preview-outlines.mjs` → outlines-preview.html (110 cards), handoff rewritten as fidelity brief. Gates green; still `wip:true`. |
| 119 | 2026-07-21 | Topothesies outlines fully swapped geoBoundaries ADM3 → **OSM admin_level=7 δήμοι** (ODbL); 325-name join collapsed to a **Wikidata-QID** join (0 unassigned); new zero-dep `osmPolygons.ts` (Overpass `out geom`→GeoJSON); absolute **200 m** coastal simplify; operator review → 22 islands DEFERRED, **Αθήνα+Πειραιάς → one `attica`**, Θήρα→Σαντορίνη; PROXIMITY_MAX_KM 938→807; ODbL attribution; 76 answers. Fidelity handoff CLOSED. |
| 118 | 2026-07-21 | Topothesies UX corrections: capital stage → plain right/wrong (no distance hint; `CapitalGuessRecord` lost `hint`, `evaluateCapitalGuess`→`{correct,known}`); **give-up** (`GIVE_UP` action + `gaveUp` flag, `TopothesiesGiveUpModal`); on-screen decorative emoji → semantic tokens (share-card Worldle grid kept). Gates green; still `wip:true`. |
| 117 | 2026-07-21 | Topothesies FINALIZED — data emission + full UI (handoff-03, `/tdd`): muni→RU by spatial nearest-match to Wikidata points; `curation.ts` assignment maps + `ANSWER_META` (101 answers); `generateTopothesies.ts` (assign→mapshaper dissolve+simplify→`project.ts`→emit); `PROXIMITY_MAX_KM 0→938`; full UI (`/topothesies`, silhouette/autocomplete/board/result, CC-BY attribution, leaderboard). (Geometry + `athina` merge + 938 all superseded by s119's OSM swap.) |
| 116 | 2026-07-21 | Topothesies emission gates resolved + handoff-03 finalized (no code): source LOCKED = geoBoundaries GRC-ADM3 (geodata.gov.gr dead; GISCO NUTS3 rejected — wrong granularity); island splits signed off (`topothesies-island-signoff.md`); key discovery = geoBoundaries has no RU layer, so emission must build a muni→RU map (became s117's spatial nearest-match). |
| 115 | 2026-07-21 | Topothesies pure game logic (handoff-02, `/tdd`): six React-free `src/games/topothesies/lib/` modules — `geo` (haversine/8-way arrow/proximity, guards `maxKm<=0`), `selectDailyPuzzle` (id-sorted `dateToIndex`), `evaluateGuess` (accent-insensitive resolve + hint), `scoring`, `topothesiesReducer` (flags DERIVED → RESTORE=replay; valid-only guesses; failed shape still enters capital), `shareText` (spoiler-free). Deviations: hint fns take `maxKm` param; state holds candidate answers. Gameplay types added. |
| 114 | 2026-07-21 | Topothesies foundation (handoff-01, `/tdd`): config scaffolding (registry wip:true 🗺️, `TOPOTHESIES` gameRules incl. `PROXIMITY_MAX_KM:0`, `SliceId`+envelope, 9th accent row) + pure pipeline logic TDD — `planDissolve` (override-dissolve to confirmed island set, drops Troizinia-Methana), `validateEmitted` (id parity/bbox/no-accents gate), `confirmedSplits.ts`; data types (`TopothesiesAnswer`/`TopothesiesShape`); **ADR 0018** (precomputed SVG paths, no client projection, flat-bundle daily render). |
| 113 | 2026-07-19 | Profile scope-clarity banner (stats/achievements are Leksokipos-only) + endgame "new content" pulse cue on the rank-ladder icon (`ScoreBar`, local `endgameSeen`, re-cues per puzzle). New ScoreBar.test.tsx. |
| 112 | 2026-07-18 | Badge display shipped (grill → 2 handoffs → built same-day): `glyph` field + Trophy Case glyphs; `player_profiles.selected_badge_id` (migration `20260718130000`, pushed) + `POST/GET /api/profile/badge` (earned-validated) + `LeaderboardBadge` chip on all boards (read-time highest-tier). `FEATURE_FLAGS.achievements` flipped `true`. Parked ideas → issue 12. |
| 111 | 2026-07-18 | Migration debt cleared + `player_words` pushed: `SUPABASE_DB_URL` added to `.env.local` (pooler needs ~60s after password reset); repaired 7 owed file versions `applied` + 5 invented MCP rows `reverted` (operator's terminal — classifier pre-blocks agent shell); history clean, `db push` safe again. `npm run db:backup` unblocked. |
| 110 | 2026-07-18 | Words-by-length lane built (pickup-03, TDD): `sanitizeFoundWords`/`bucketWordsByLength`/`planWordsMerge`, `POST /api/words`, `GET /api/profile/words` (invoker-rights RPC), 4th `useAchievementSync` lane, `WordsByLengthCard` on /profile — capture **and** display behind `FEATURE_FLAGS.achievements`. Migration `20260718120000` (`player_words`) committed; applied in s111. |
| 109 | 2026-07-18 | Podium counts (pickup-02, TDD): `countPodiumFinishes` one-pass competition ranking (90/90/80 → the 80 is 3rd, no 2nd); stats route adds 2nd/3rd counts off the existing fetch (zero new queries); strip Πρωτιές → combined 🥇🥈🥉 «Βάθρο» cell. |
| 108 | 2026-07-18 | Perfect-round/Τζιμάνι removed on all four surfaces (pickup-01): catalog, stats/strip, 🏛️ leaderboard glyphs, whole `isPerfect` wire; Leksokipos completion kept as `allWordsFound` (🏆 win glyph). Prod DELETE was a no-op (0 rows) — id clean for re-award. ADR 0013: first deliberate frozen-id exception. |
| 107 | 2026-07-18 | Docs-only close-out of the achievements/stats handoffs → pickups 01/02/03 + `badges-parked.md`. Decisions: Τζιμάνι removed completely, words-by-length is Leksokipos-only, consecutive-submissions stat dropped; B2/B1 smoke check → issue 11 (ready-for-human). |
| 106 | 2026-07-17 | Fixed 28 zero-pangram Leksokipos boards (issue 09): ADR 0015 re-sync had silently stripped last pangrams. Regenerated the 26 future boards (2 already-played dates left, allowlisted); new `everyPuzzleHasPangram.test.ts` drift guard; re-sync adapter now warns when a removal strips a board's last pangram. |
| 105 | 2026-07-17 | Redesign-prep doc session closing the 01/02/03 handoff chain: ADR 0008 extension (status/shape tokens, `--container-game`), ADR 0009 layout seam (GamePageShell/GameHeader, 8/8 accent map, redesign surface). FeedbackBanner tokenization parked in goals. |
| 104 | 2026-07-17 | Handoff 03: status tokens (`warning`/`info` trios + `danger`/`success` companions — NominationModal to 0 `dark:` pairs), `btnInfo`/`chipWarning` recipes, shape tokens (`--radius-card/control`, `--shadow-card`, byte-identical compile), accent rows for stavrolekso (sky) + leksikastirio (indigo) → all 8 surfaces. |
| 103 | 2026-07-17 | Handoff 02: recipe leaks closed — ~9 inline inverted buttons → `btnModalSubmit`/`btnModalPrimary` (recipe owns colour/type/radius, call site owns layout), 4×-pasted `tooltipBubble`, `cardShell`/`cardShellInteractive`; recipes.test.ts contracts; greps clean (0 leaks left). |
| 102 | 2026-07-17 | Handoff 01: game-page frame → `GamePageShell` + `GameHeader` + `max-w-game` (`--container-game: 24rem`); `max-w-sm` swept to 0 in shipped `.tsx` with new `noLiteralColumnWidth.test.ts` guard. Leksokipos kept its bespoke full-bleed wrapper (operator decision); only its header moved to GameHeader. |
| 101 | 2026-07-17 | Nominations dedup backstops verified against the live DB (issue 08): real `23505`s for the pending (word,direction) partial unique + the vote pair (mocked tests only proved route branches). Sentinel word seeded + wiped both sides; zero sentinels survive. |
| 100 | 2026-07-17 | CI at last (issue 03): `ci.yml` (eslint + tsc + vitest; no live-DB secrets by design); `loadEnv` in vitest.config forwards the 3 Supabase keys so the live-DB suites execute locally for the first time; `cleanupScoresLiveDb` rewritten to seeded-sentinel effect assertions; 24 stale tsc errors fixed in fixtures only. |
| 99 | 2026-07-17 | Vres Tin Frasi short guess pools (`words-2/3.json`) joined the ADR 0015 registry via a `vrestifrasi` adapter (`lengthSlicedWords.ts`, issue 05); 118 blocklisted pool words pinned as a shrink-only ratchet. Separate un-ticketed find: 147/498 phrases already unwinnable vs the 2–8 pool. |
| 98 | 2026-07-17 | `assertNotBlocked` gates the apply orchestrator (issue 07): approve-then-blocklist can no longer write a proper noun — throws and stops the whole batch (a skip would re-trigger forever); exempts the deferred overlap; never gates removes. |
| 97 | 2026-07-17 | Blocklist ∩ dictionary became a guarded shrink-only invariant (issue 06): `ατλασ`/`ορκα` removed as false positives (nominatable again); `DEFERRED_BLOCKLIST_DICTIONARY_OVERLAP` (14 month names) exported and pinned to *equal* the overlap exactly. |
| 96 | 2026-07-17 | Stavrolekso `edit_pin` no longer readable by the public anon key (issue 04): migration `20260717120000` — column-level REVOKE/GRANT for anon **and** authenticated; PATCH PIN lookup → service role. 5 live-DB + 2 unit locks; table had 0 rows, nothing to rotate. Classifier trap re-hit (recorded in `/project-mcp` #3). |
| 95 | 2026-07-17 | dev→main production deploy: pre-merge verification, operator merged `3614145`, then the deploy-coupled vrestifrasi flip `20260715120100` applied via dashboard SQL editor (classifier pre-blocked MCP). Verified live 2/3/6 → 5/4/1; ADR 0014 fully live. |
| 94 | 2026-07-17 | Full documentation audit — every doc cross-checked vs code + live DB. Fixed: README (rank ladder fully wrong, leaderboard direction, missing boards/games), CLAUDE.md, project-mcp, goals/CONTEXT (**Offline Lock/Outbox marked designed-NOT-built**), memory coverage map completed. Filed issue 05; reconstructed unlogged commit `de4810a`. |
| 92 | 2026-07-16 | All four DB-hardening handoffs applied (4 migrations, 4 commits): transfer codes server-only + atomic claim + `crypto.getRandomValues`; anon RLS narrowed per-command (append-only enforced, ADR 0013 amended); dedup backstops (vote UNIQUE + pending partial unique, POST 23505 → 409 already_pending); `community_puzzle_status` PG enum (regen types caught a raw `?status=` `.eq()` bug). |
| 91 | 2026-07-16 | Full DB review (live schema × repo wiring) → 4 handoffs, no code. Worst find: Stavrolekso creator edit silently no-oped in prod (anon UPDATE, RLS matched 0 rows, `ok:true`); also transfer_codes device_uuid oracle, ADR 0013 anon-DELETE contradiction, missing dedup backstops. |
| 90 | 2026-07-16 | Supabase types generated + wired into the compiler (ADR 0017): the "brittle" blocker didn't reproduce on 2.105.4 so `QueryBuilder = any` was deleted; compiler found 3 routes passing URL strings into `bigint` `.eq()`. Drift guard deferred (vitest had never run in CI → ticket 03). Lesson: a type nothing enforces will rot. |
| 89 | 2026-07-16 | Route envelope (ADR 0016): `parseJson`/`requireAdmin` + two error channels (`jsonError` codes w/ detail logged-not-leaked; `jsonMessage` for authored copy incl. Greek UI strings) — 14 routes + `communityPuzzleLifecycle` migrated. Breaking on purpose: admin secret body → header, 403 → 401. Item 5: `mergeLengthScore` fold extracted. |
| 88 | 2026-07-16 | One `todayISO()` in `src/lib/puzzleDate.ts` (~12 re-derivations collapsed); UTC rollover deliberately preserved + pinned by test. Real bug fixed: LeksodromiaBoard passed the puzzle date as `today`, mis-anchoring the 7-day strip on past puzzles. |
| 87 | 2026-07-16 | Premade-data re-sync registry (ADR 0015): `scripts/lib/resync/` per-game adapters (incl. Leksoplegma `bonusWords` + Leksodromia `anagramAlternates`, both silently stale before), `apply-nominations` → tsx with game-logic mirrors deleted, `premadeDataConsistency.test.ts` drift guard. Traps: tsx compiles CJS here; 3 non-normalised dict entries. Findings: `ιουνιοσ` needs re-curation, `σταυλου` nominated twice. |
| 86 | 2026-07-16 | Leksikastirio nomination guards: `normalizeLetters` across POST/lookup/modal (accent variants now collapse to one pending); 16,947-entry proper-noun blocklist (422 `blocked_word`, add-only, 🚫 banner); burst-duplicate fix via synchronous `busyRef` lock (ΑΓΟΡΑΡΟΣ ×6 in 32 ms diagnosed — held-Enter key-repeat). DB unique backstop deferred (landed s92). |
| 85 | 2026-07-15 | DB-review follow-ups (grilled handoff, then deleted): `game_scores` read indexes (`20260715120000`, applied via `execute_sql` → needs `migration repair`); Vres→higher-is-better (**ADR 0014**, flip migration `20260715120100`); **Πρωτιές** first-place count (pure `countFirstPlaceFinishes`, cross-device stats fetch, 5th strip cell — no schema). Parked: per-game max stat, placement badges, `player_placements`. |
| 84 | 2026-07-15 | Leksokipos **soft cap**: `softCap` log curve (knee 400, k 250, simulated on 1008 puzzles) replaces `MAX_SCORE_CAP:600` clip — genius target tracks puzzle richness (~460–685 normal, monsters ~750–986). |
| 83 | 2026-07-15 | DB schema review → handoff (no code): cron concern moot (verified prod), growth/no-total-score decisions re-affirmed; real findings = missing `game_scores` indexes + placement unbuilt (became session 85). |
| 74–82 | 2026-07-13/15 | **Architecture-review + two-new-games era.** Four arch-review seams landed back to back: `usePlayerIdentity` (one hook bundling migrate + identity/profile/auth, 6 call sites), the **session spine owning score-posting** (`rawDispatch` vs wrapped dispatch flipping `hasLiveActedRef`; shared `useLiveScorePost`), **one `GameLeaderboardModal` + `GAME_LEADERBOARD_CONFIG`** replacing 6 per-game wrappers (type trap: annotate the map `Record<LeaderboardGameId,…>`, never `satisfies`), and the Daily-Answer seam `answerPools.ts`. **Λεξοδρομία** (decay-to-floor scoring, refresh-proof decay clock) and **Λεξόπλεγμα** (pure graph/scoring lib, single `TRACE_WORD` reducer seam, offline generator) both built `/tdd` in 7 slices each off design grills, QA-polished, and **graduated `wip:false` → 7 live games**; Leksodromia gained Second Chance. Fluid CPU prerender read-out: **VERDICT FIXED ✅**, `[center]/[outer]` 44→~1 inv/60s; Vercel Pro. |
| 66–73 | 2026-07-06/13 | **Achievements B1/B2 + Fluid CPU era.** B1: points tier + unlock toast + TrophyCase progress, `useAchievementSync` points/toast lanes with earned-at-mount suppression. B2: `player_pangrams` append-only find-set (ADR 0013), `POST /api/pangrams`, `detectEarnedPangramTiers`, 3rd sync lane + self-heal; its handoff was code-verified first (claims accurate, one R6 contradiction fixed). Fluid: **lazy-load words-el** (async `await import()` on cache-miss only, 19.9 MB chunk out of the route) then **prerender daily combos** (`generateStaticParams` → 1008 SSG pages, custom combos keep ISR). Plus the design grills that became Λεξοδρομία/Λεξόπλεγμα. |
| 54–65 | 2026-07-02/05 | **Identity + Profile Page era.** Sign-in Restore: design grill → **ADR 0012** (auth = the durable anchor, restore adopts the account DeviceId), `planScoreMerge`, `adoptDeviceIdentity`, `identity_audit` at link-time, hard `reloadApp()` on Disconnect. Epic B COMPLETE: `/profile` route + 3 entry points, `GET /api/profile/stats` + `aggregateLifetimeStats` + strip, page-local TrophyCase. Fixed `game_scores` pruning that contradicted ADR 0012 append-forever — **cron never deletes scores**, regression-locked. Fluid: puzzle-index (route chunk 22MB→0.2MB) + ISR 604800. ADR 0008 palette sweep + `noRawPaletteClasses` guard; `GameId`→`SliceId`; test-suite audit → soul.md's "coverage never goes down". |
| 43–53 | 2026-06-22/29 | **Design-system + nomination-pipeline era.** Full design-token consolidation (**ADR 0008**) + rank rename (`RANKS` single source) + `platform.ts`; UI consolidation (**ADR 0009**) — per-game `--game-accent`, shared `Modal` across 9 modals, recipes split platform vs leksokipos. `consumeApprovedPuzzle` lifecycle + shared `useGuessRound` spine + Leksokipos `sync.ts`. Fixed broken score cleanup (`void` not `await`) → `cleanup-scores` cron. Wordlist proper-noun cleanup: 16,933 removals (812k→795k) via a Hunspell capitalisation signal. Nomination apply pipeline (`npm run apply-nominations[:dry]` + surgical re-sync), re-proposal warning flow, `useDayChange` auto-advance. |
| 1–42 | 2026-05-12 / 05-30 | **Foundation + rebrand + community era.** Shell, routing, persistence, types · Leksiarxeio · Theming · Leksindeseis · Greeklish URLs · puzzle quality filter · word suggestions · per-puzzle leaderboard + 7-day strip · mobile layout · the no-accent invariant · `maxScore` cap. Shared-lib consolidation: `postScore`+`upsertAndClean`, `useRoundPersistence`, `useLeaderboard`, `isDailyPuzzle`/`isISODate`; Connections leaderboard; the first Fluid CPU mitigations (`validWordsCache`, ISR, Edge routes); `CONTEXT.md` created. Then the rebrand to Greek names (routes + identifiers; puzzle IDs frozen); `FlowerGrid` SVG + variant presets; `useGameIdentity`; `useScoreSubmission` unified; dark mode (**ADR 0002**) + Shell drawer split; community puzzles (review routes, async loaders, admin tabs); Leksokipos restore slimmed (**ADR 0003**); **Vres Tin Frasi** (4th game) built; game-state restore + FOUC fixes; Google OAuth augments device identity (`useAuth`, PKCE callback, `/api/auth/link`, **ADR 0007**). |

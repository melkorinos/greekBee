# Agent Log — Greek Word Games Platform

> Newest-first. Full detail for the two most recent sessions only; everything older is a one-line row in the table.
> **Rule (2026-07-18):** keep this file under **120 lines** — the end-of-session Dream (soul.md) demotes older entries and era-groups the oldest rows. Git history keeps the full text.

---

## Session 112 — 2026-07-18: badge display shipped (grill → 2 handoffs → built) — ⚠️ migration NOT yet pushed

Grilled the badge plan with the operator (all decisions locked: badge shows on all six boards; emoji glyphs interim; select the *achievement*, highest earned tier auto-displays; opt-in default-none, tap-again clears; picker = Trophy Case; server validates ownership). Two handoffs written and both **implemented same-day by pickup agents**: (A) `glyph` field + Trophy Case glyphs (🌱👑🚂🌾✍️💎); (B) `player_profiles.selected_badge_id` (migration `20260718130000`) + `POST/GET /api/profile/badge` (earned-validated, lazy profile upsert) + Trophy Case tap-select/clear (optimistic w/ rollback) + `LeaderboardBadge` chip after the name on all boards via game-scores GET fan-out (read-time highest-tier resolution, dangling → no badge). Gates green (1963 tests). Docs: ADR 0013 amendment, CONTEXT.md (Display Badge glossary + table row), memory.md coverage rows, goals.md. All three badge handoffs **deleted**; still-parked ideas → issue `12-badge-ideas-parked.md` (podium tiers = agreed next promotion). Later same session: migration `20260718130000` **pushed** (plain `db push --db-url`, clean history held) and **`FEATURE_FLAGS.achievements` flipped to `true`** (operator decision) — Trophy Case + Words-by-Length + toasts + the recording pipeline are live on next deploy; issue 11's smoke check is now unblocked. Gates re-run green after the flip.

## Session 111 — 2026-07-18: migration debt cleared + `player_words` pushed — history clean, `db push` safe again

Executed the repair-then-push plan. `SUPABASE_DB_URL` (session-pooler URI) added to `.env.local` — **a dashboard password reset takes ~60 s to propagate to the pooler** (auth kept failing until it did). Repaired the 7 owed file versions `--status applied`; push then refused because the **5 invented MCP-apply rows** had no local files → repaired those `--status reverted` (operator's terminal — the auto-mode classifier pre-blocked both that repair AND `db push` from the agent shell; trap recorded in `/project-mcp` #3). Push applied `20260718120000_add_player_words.sql` (trailing Docker warning = harmless catalog caching). Verified live: table + RLS (SELECT/INSERT-only), invoker-rights RPC, history top = `20260718120000`. Full suite: **1921/1922 green incl. the 3 live-DB words assertions**; the 1 failure (leksodromia board "skips never re-post") is a load-flake — passes 13/13 in isolation. Docs: `/project-mcp` updated (14 tables, repair-debt paragraph → resolved, classifier trap extended), pickup-03 handoff deleted, memory.md Supabase row updated. **Bonus unblocked:** `npm run db:backup` needed this same env var (issue 02's manual-dump path now works).

---

## Older Sessions

| Session | Date | Summary |
|---------|------|---------|
| 110 | 2026-07-18 | Words-by-length lane built (pickup-03, TDD): `sanitizeFoundWords`/`bucketWordsByLength`/`planWordsMerge`, `POST /api/words`, `GET /api/profile/words` (invoker-rights RPC), 4th `useAchievementSync` lane, `WordsByLengthCard` on /profile — capture **and** display behind `FEATURE_FLAGS.achievements`. Migration `20260718120000` (`player_words`) committed; applied in s111. |
| 109 | 2026-07-18 | Podium counts (pickup-02, TDD): `countPodiumFinishes` one-pass competition ranking (90/90/80 → the 80 is 3rd, no 2nd); stats route adds 2nd/3rd counts off the existing fetch (zero new queries); strip Πρωτιές → combined 🥇🥈🥉 «Βάθρο» cell. |
| 108 | 2026-07-18 | Perfect-round/Τζιμάνι removed on all four surfaces (pickup-01): catalog, stats/strip, 🏛️ leaderboard glyphs, whole `isPerfect` wire; Leksokipos completion kept as `allWordsFound` (🏆 win glyph). Prod DELETE was a no-op (0 rows) — id clean for re-award. ADR 0013: first deliberate frozen-id exception. |
| 107 | 2026-07-18 | Docs-only close-out of the achievements/stats handoffs → pickups 01/02/03 + `badges-parked.md`. Decisions: Τζιμάνι removed completely, words-by-length is Leksokipos-only, consecutive-submissions stat dropped; B2/B1 smoke check → issue 11 (ready-for-human). |
| 106 | 2026-07-17 | Fixed 28 zero-pangram Leksokipos boards (issue 09): ADR 0015 re-sync had silently stripped last pangrams. Regenerated the 26 future boards (2 already-played dates left, allowlisted); new `everyPuzzleHasPangram.test.ts` drift guard; re-sync adapter now warns when a removal strips a board's last pangram. |
| 105 | 2026-07-17 | Redesign-prep doc session closing the 01/02/03 handoff chain: ADR 0008 extension (status/shape tokens, `--container-game`), ADR 0009 layout seam (GamePageShell/GameHeader, 8/8 accent map, redesign surface). FeedbackBanner tokenization parked in goals. |
| 104 | 2026-07-17 | Handoff 03: status tokens (`warning`/`info` trios + `danger`/`success` companions — NominationModal to 0 `dark:` pairs), `btnInfo`/`chipWarning` recipes, shape tokens (`--radius-card/control`, `--shadow-card`, byte-identical compile), accent rows for stavrolekso (sky) + leksikastirio (indigo) → all 8 surfaces. |
| 103 | 2026-07-17 | Handoff 02: recipe leaks closed — ~9 inline inverted buttons → `btnModalSubmit`/`btnModalPrimary` (recipe owns colour/type/radius, call site owns layout), 4×-pasted `tooltipBubble`, `cardShell`/`cardShellInteractive`; recipes.test.ts contracts; greps clean (0 leaks left). |
| 102 | 2026-07-17 | Handoff 01: game-page frame → `GamePageShell` + `GameHeader` + `max-w-game` (`--container-game: 24rem`); `max-w-sm` swept to 0 in shipped `.tsx` with new `noLiteralColumnWidth.test.ts` guard. Leksokipos kept its bespoke full-bleed wrapper (operator decision); only its header moved to GameHeader. |
| 101 | 2026-07-17 | Nominations dedup backstops verified against the live DB (issue 08): real `23505`s for the pending (word,direction) partial unique + the vote pair (mocked tests only proved route branches). Sentinel word seeded + wiped both sides; zero sentinels survive. |
| 100 | 2026-07-17 | CI at last (issue 03): `ci.yml` (eslint + tsc + vitest; no live-DB secrets by design); `loadEnv` in vitest.config forwards the 3 Supabase keys so the live-DB suites execute locally for the first time; `cleanupScoresLiveDb` rewritten to seeded-sentinel effect assertions; 24 stale tsc errors fixed in fixtures only. |
| 99 | 2026-07-17 | Vres Tin Frasi short guess pools (`words-2/3.json`) joined the ADR 0015 registry via a `vrestifrasi` adapter (`lengthSlicedWords.ts`, issue 05); 118 blocklisted pool words pinned as a shrink-only ratchet. Separate un-ticketed find: 147/498 phrases already unwinnable vs the 2–8 pool. |
| 98 | 2026-07-17 | `assertNotBlocked` gates the apply orchestrator (issue 07): approve-then-blocklist can no longer write a proper noun — throws and stops the whole batch (a skip would re-trigger forever); exempts the deferred overlap; never gates removes. |
| 97 | 2026-07-17 | Blocklist ∩ dictionary became a guarded shrink-only invariant (issue 06): `ατλασ`/`ορκα` removed as false positives (nominatable again); `DEFERRED_BLOCKLIST_DICTIONARY_OVERLAP` (14 month names) exported and pinned to *equal* the overlap exactly. |
| 96 | 2026-07-17 | Stavrolekso `edit_pin` no longer readable by the public anon key (issue 04): migration `20260717120000` — column-level REVOKE/GRANT for anon **and** authenticated; PATCH PIN lookup → service role. 5 live-DB + 2 unit locks; table had 0 rows, nothing to rotate. Classifier trap re-hit (recorded in `/project-mcp` #3). |
| 95 | 2026-07-17 | dev→main production deploy: pre-merge verification, operator merged `3614145`, then the deploy-coupled vrestifrasi flip `20260715120100` applied via dashboard SQL editor (classifier pre-blocked MCP). Verified live 2/3/6 → 5/4/1; ADR 0014 fully live. |
| 94 | 2026-07-17 | Full documentation audit — every doc cross-checked vs code + live DB. Fixed: README (rank ladder fully wrong, leaderboard direction, missing boards/games), CLAUDE.md, project-mcp, goals/CONTEXT (**Offline Lock/Outbox marked designed-NOT-built**), memory coverage map completed. Filed issue 05; reconstructed unlogged commit `de4810a`. |
| 92 | 2026-07-16 | All four DB-hardening handoffs applied (4 migrations, 4 commits): transfer codes server-only + atomic claim + `crypto.getRandomValues`; anon RLS narrowed per-command (append-only enforced, ADR 0013 amended); dedup backstops (vote UNIQUE + pending partial unique, POST 23505 → 409 already_pending); `community_puzzle_status` PG enum (regen types caught a raw `?status=` `.eq()` bug). |
| 91 | 2026-07-16 | Full DB review (live schema × repo wiring) → 4 handoffs, no code. Worst find: Stavrolekso creator edit silently no-oped in prod (anon UPDATE, RLS matched 0 rows, `ok:true`); also transfer_codes device_uuid oracle, ADR 0013 anon-DELETE contradiction, missing dedup backstops. |
| 90 | 2026-07-16 | Supabase types generated + wired into the compiler (ADR 0017): the "brittle" blocker didn't reproduce on 2.105.4 so `QueryBuilder = any` was deleted; compiler found 3 routes passing URL strings into `bigint` `.eq()`. Drift guard deferred (vitest had never run in CI → ticket 03). Lesson: a type nothing enforces will rot. |
| 89 | 2026-07-16 | Route envelope (ADR 0016): `parseJson`/`requireAdmin` + two error channels (`jsonError` codes w/ detail logged-not-leaked; `jsonMessage` for authored copy incl. Greek UI strings) — 14 routes + `communityPuzzleLifecycle` migrated. Breaking on purpose: admin secret body → header, 403 → 401. Item 5: `mergeLengthScore` fold extracted. |
| 88 | 2026-07-16 | One `todayISO()` in `src/lib/puzzleDate.ts` (~12 re-derivations collapsed); UTC rollover deliberately preserved + pinned by test. Real bug fixed: LeksodromiaBoard passed the puzzle date as `today`, mis-anchoring the 7-day strip on past puzzles. |
| 87 | 2026-07-16 | Premade-data re-sync registry (ADR 0015): `scripts/lib/resync/` per-game adapters (incl. Leksoplegma `bonusWords` + Leksodromia `anagramAlternates`, both silently stale before), `apply-nominations` → tsx with game-logic mirrors deleted, `premadeDataConsistency.test.ts` drift guard. Traps: tsx compiles CJS here; 3 non-normalised dict entries. Findings: `ιουνιοσ` needs re-curation, `σταυλου` nominated twice. |
| 86 | 2026-07-16 | Leksikastirio nomination guards: `normalizeLetters` across POST/lookup/modal (accent variants now collapse to one pending); 16,947-entry proper-noun blocklist (422 `blocked_word`, add-only, 🚫 banner); burst-duplicate fix via synchronous `busyRef` lock (ΑΓΟΡΑΡΟΣ ×6 in 32 ms diagnosed — held-Enter key-repeat). DB unique backstop deferred (landed s92). |
| 85 | 2026-07-15 | DB-review follow-ups (grilled handoff, then deleted): `game_scores` read indexes (`20260715120000`, applied via `execute_sql` → needs `migration repair`); Vres→higher-is-better (**ADR 0014**, flip migration `20260715120100`); **Πρωτιές** first-place count (pure `countFirstPlaceFinishes`, cross-device stats fetch, 5th strip cell — no schema). Parked: per-game max stat, placement badges, `player_placements`. |
| 84 | 2026-07-15 | Leksokipos **soft cap**: `softCap` log curve (knee 400, k 250, simulated on 1008 puzzles) replaces `MAX_SCORE_CAP:600` clip — genius target tracks puzzle richness (~460–685 normal, monsters ~750–986). |
| 83 | 2026-07-15 | DB schema review → handoff (no code): cron concern moot (verified prod), growth/no-total-score decisions re-affirmed; real findings = missing `game_scores` indexes + placement unbuilt (became session 85). |
| 82 | 2026-07-15 | Daily-Answer seam (arch-review #4): `answerPools.ts` (`getSameDayFallbackAnswers`) — Leksodromia/Leksoplegma stop re-deriving pool indexing + the Fluid-safe import block. Pure restructure. |
| 81 | 2026-07-15 | **One GameLeaderboardModal + config** (arch-review #3): 6 per-game leaderboard wrappers deleted → shared `GameLeaderboardModal` + `GAME_LEADERBOARD_CONFIG`; 8 call sites migrated; type trap: annotate map `Record<LeaderboardGameId,…>`, not `satisfies`. |
| 80 | 2026-07-15 | **Session spine owns score-posting** (arch-review #2): round hooks split `rawDispatch` vs wrapped dispatch flipping `hasLiveActedRef`; new shared `useLiveScorePost.ts` — both Boards deleted their ref+effect plumbing. |
| 79 | 2026-07-15 | **`usePlayerIdentity`** deep module (arch-review #1): one hook bundles migrate + useGameIdentity/useProfile/useAuth + `saveName`, returns `leaderboardProps`; adopted at 6 side-effect-free call sites. |
| 78 | 2026-07-14 | Λεξόπλεγμα extras reinstated + soft collapse (prod grill); **Leksodromia Second Chance** (first «Επόμενο» requeues, second final); greeklish UI renames; continuous score posting both games. |
| 77 | 2026-07-14 | Λεξόπλεγμα/Λεξοδρομία QA polish (counter bug, tap-to-remove, two-row rack); **both graduated wip:false → 7 live games**. |
| 76 | 2026-07-14 | Fluid CPU prerender read-out — **VERDICT FIXED ✅**: `[center]/[outer]` 44→~1 inv/60s, ≈0.6 min/day vs 10 baseline. Vercel Pro upgrade. `/project-mcp` CLI section added. |
| 75 | 2026-07-14 | **Λεξόπλεγμα built** (`/tdd`, 7 slices): pure graph/scoring lib, single `TRACE_WORD` reducer seam, offline generator (batch 200), same-day-answer guard, SVG live-edge components. |
| 74 | 2026-07-13 | **Λεξοδρομία built** (`/tdd`, 7 slices): decay-to-floor scoring, seeded daily selection + scramble, refresh-proof decay clock (`useElapsedClock`), Board/recap/leaderboard. |
| 72–73 | 2026-07-13 | Design grills → handoffs for both new games (zanagrams reverse-engineer; decay scoring, name-first blocker). Built in 74/75. |
| 71 | 2026-07-10 | **Prerender daily combos** (Fluid #2): `getPrebuiltPuzzleParams` + `generateStaticParams` → 1008 pages SSG; custom combos keep ISR. |
| 70 | 2026-07-08 | Fluid read-out + **lazy-load words-el**: async `await import()` on cache-miss only; 19.9 MB chunk out of the route. |
| 69 | 2026-07-07 | **Achievements B2 — pangram tier** (`/tdd`): `player_pangrams` append-only find-set (ADR 0013), `POST /api/pangrams`, `detectEarnedPangramTiers`, 3rd sync lane + self-heal, TrophyCase generalized. |
| 68 | 2026-07-06 | B2 handoff code-verification review: claims accurate; fixed R6 contradiction, added R2 input guards; self-heal rides the ONE stats fetch. |
| 66 | 2026-07-06 | **Achievements B1** (`/tdd`): points tier + unlock toast + TrophyCase progress; `useAchievementSync` points+toast lanes (earned-at-mount suppression). |
| 65 | 2026-07-05 | Fixed `game_scores` prune contradicting ADR 0012 append-forever (issue 03): cron never deletes `game_scores`; window-filter regression-locked. |
| 64 | 2026-07-05 | Fluid CPU: `/leksokipos` puzzle-index (route chunk 22MB→0.2MB) + `[center]/[outer]` ISR 604800; `consumeApprovedPuzzle`-per-view bug handed off. |
| 63 | 2026-07-04 | **Feedback feature**: FormSubmit AJAX relay, `FeedbackModal` via Shell "Βοήθεια", 60s throttle, `btnModalPrimary` extracted. |
| 62 | 2026-07-03 | Consolidation-file consistency: config single-sources enforced; `GameId`→`SliceId` rename; ADR 0008 palette sweep + `noRawPaletteClasses.test.ts` guard. |
| 61 | 2026-07-03 | **Epic B — Profile Page + Trophy Case COMPLETE** (`/tdd`): `/profile` route, 3 entry points, `GET /api/profile/stats` + `aggregateLifetimeStats` + strip, page-local TrophyCase. |
| 60 | 2026-07-03 | **Epic A COMPLETE** (migration pushed+verified): `identity_audit` at link-time; hard `reloadApp()` on Disconnect. Migration `20260703092500`. |
| 59 | 2026-07-03 | Epic A slices 3+4: `disconnectIdentity()` full-reset; Google sign-in visibility rule wired into all 4 boards. |
| 58 | 2026-07-03 | Profile Page grill → handoff, zero code. Decisions table, catalog draft, restore→/profile redirect. CONTEXT glossary terms. |
| 56–57 | 2026-07-02 | Sign-in Restore: design grill → **ADR 0012** (auth = durable anchor, restore adopts DeviceId), then slices 1–2 (JWT identity source, `planScoreMerge`, `adoptDeviceIdentity`; fixed silent backfill bug). |
| 55 | 2026-07-02 | Test-suite audit: gap-fill + dup cleanup; soul.md rule "coverage never goes down"; new vrestifrasi/useProfile/useLeaderboardProfile suites. |
| 54 | 2026-07-02 | Architecture: 4 pure community-puzzle `validateSubmission` adapters; Stavrolekso PATCH edit-hole closed; maker/server dedup. |
| 53 | 2026-06-29 | UI consolidation (**ADR 0009**): per-game `--game-accent` token; shared `Modal` primitive (9 modals); recipes split platform vs leksokipos. |
| 51–52 | 2026-06-28/29 | Bug fixes: past-puzzle nav (mount-redirect guard, remount key, `shouldSave`); leaderboard "back to today" link condition. |
| 50 | 2026-06-28 | Architecture: `consumeApprovedPuzzle` lifecycle; shared `useGuessRound` spine; Leksokipos `sync.ts` seam. |
| 49 | 2026-06-27 | Leksokipos UI polish: `btnHeaderIcon` recipe, copy-icon share, Greek feedback msgs, two-phase GiveUpModal. |
| 48 | 2026-06-27 | Fixed broken score cleanup (`void` not `await`); new `cleanup-scores` cron route (CRON_SECRET, service role, >7d) + daily `vercel.json` cron. |
| 47 | 2026-06-27 | Rank rename (`RANKS` single source) + full design-token consolidation (**ADR 0008**); `platform.ts`. |
| 46 | 2026-06-24/26 | Wordlist proper-noun cleanup: 16,933 removals from `words-el.json` (812k→795k) via Hunspell capitalisation signal; puzzles re-synced. |
| 43–45 | 2026-06-22 | Nomination apply pipeline (`npm run apply-nominations[:dry]` + surgical re-sync); re-proposal warning flow; NYT brand scrub; pinch-zoom lock; `useDayChange` auto-advance. |
| 40–42 | 2026-05-28/30 | Vres Tin Frasi (4th game) built; game-state restore + FOUC fixes; Google OAuth augments device identity (`useAuth`, PKCE callback, `/api/auth/link`, ADR 0007). |
| 36–39 | 2026-05-27/28 | Dark mode (ADR 0002) + Shell drawer split; community puzzles (review routes, async loaders, admin tabs); Leksokipos restore slimmed (ADR 0003). |
| 31–35 | 2026-05-22/26 | Platform rebrand to Greek names (routes+identifiers; puzzle IDs frozen); `FlowerGrid` SVG + variant presets; `useGameIdentity`; `useScoreSubmission` unified. |
| 22–30 | 2026-05-20/22 | Shared-lib consolidation era: `postScore`+`upsertAndClean`, `useRoundPersistence`, `useLeaderboard`, `isDailyPuzzle`/`isISODate`. Connections leaderboard. Fluid CPU mitigations (`validWordsCache`, ISR, Edge routes). `CONTEXT.md` created. |
| 1–21 | 2026-05-12–19 | Foundation (shell, routing, persistence, types) · Leksiarxeio · Theming · Leksindeseis · Greeklish URLs · quality filter · suggestions · per-puzzle leaderboard + 7-day strip · mobile · no-accent invariant · `maxScore` cap. |
